services:
  kernel.event_listener.route_match:
    class: Opstalent\SecurityBundle\EventListener\RouteMatchListener
    arguments:
      - "@router"
      - "@security.token_storage"
    tags:
      - { name: kernel.event_listener, event: kernel.controller, method: onKernelRequest, priority: 99 }